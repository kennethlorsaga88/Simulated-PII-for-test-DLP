# Simulated-PII-for-test-DLP
Script to generated different file type include make up PII


# Run this in the same PowerShell window before your script:
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# Then run:
.\Generate-DLP-TestData.ps1 -OutputDir . -HeavyCount 150
